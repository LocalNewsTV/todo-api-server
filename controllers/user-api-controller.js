export const signIn = (req, res) => {
  res.status(200).send('Successful API POST Request - Sign in');
}

export const signUp = (req, res) => {
  res.status(200).send('Successful API POST Request - Sign up');
}